# v1.1.0
 * Added click sound system — plays a customizable press and release sound at the exact tick the input occurs
 * Added TPS mismatch warning — zzBot now saves the TPS used during recording and warns you if it differs when replaying
 * Changed frame stepper keybinds to F (advance) and G (exit), removed go-back feature
 * Swing copter added to trajectory prediction game mode list

# v1.0.1
 * Fixed bugs

# v1.0.0
 * Initial release
 * Frame-accurate replay system with recording and playback
 * Save and load macros as .zzb files
 * Speed control presets (0.01x to 2.0x)
 * FPS unlocker (60, 120, 240, 480, unlimited)
 * Physics TPS modifier
 * Frame stepper (X = forward, C = back, V = exit)
 * Trajectory prediction overlay
 * Hitbox visualiser and hitbox trail
 * Control panel integrated into the pause menu
